<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Список логин+пароль персонала на фестиваль
    |--------------------------------------------------------------------------
    |
    | Путь к НЕкоммитимому файлу со списком сотрудников (логин/пароль/роль),
    | который заводится перед фестивалем командой `php artisan tickets:crateUser`
    | (или сидером StaffUsersSeeder). Пароли в файле — открытым текстом, при
    | заведении хешируются. Сам файл в .gitignore (в репо — только .example).
    |
    | Формат файла: см. database/seeders/data/staff_users.example.php
    | (тесты переопределяют этот путь на временный файл, чтобы НЕ писать в боевой).
    |
    */
    'staff_users_path' => env(
        'BAZA_STAFF_USERS_PATH',
        database_path('seeders/data/staff_users.php')
    ),

    /*
    |--------------------------------------------------------------------------
    | Текущий фестиваль по умолчанию (фундамент сервиса фестивалей, TD-48)
    |--------------------------------------------------------------------------
    |
    | Единый источник правды для UUID «текущего фестиваля». Раньше эта константа
    | была зашита (`9d679bcf-…`) в ~10 репозиториях/командах — менялась правкой
    | кода каждый фестиваль. Теперь fallback берётся отсюда (env), а реальный
    | фестиваль смены приходит из реестра `festivals` (выбор при открытии смены).
    | id == festival_id заказа/билета в org (общий идентификатор каталога).
    |
    */
    'default_festival_id' => env(
        'BAZA_DEFAULT_FESTIVAL_ID',
        '9d679bcf-b438-4ddb-ac04-023fa9bff4b8'
    ),

    /*
    |--------------------------------------------------------------------------
    | Строгая изоляция КПП по фестивалю смены (TD-48, PR-3+)
    |--------------------------------------------------------------------------
    |
    | OFF (по умолчанию) — поведение как раньше: скан/поиск/впуск работают по
    | fallback-фестивалю, ничего не меняется. ON — скан/поиск/впуск/счётчики
    | ограничены фестивалём ОТКРЫТОЙ СМЕНЫ сотрудника, билет чужого фестиваля
    | не пускается. Включать только после прогона на стенде.
    |
    */
    'festival_isolation' => (bool) env('BAZA_FESTIVAL_ISOLATION', false),

    /*
    | Бутстрап-данные реестра фестивалей (FestivalSeeder) — чтобы система не была
    | пустой до синка из org. Реальные название/год приходят из org-реплики.
    */
    'default_festival_name' => env('BAZA_DEFAULT_FESTIVAL_NAME', 'Текущий фестиваль'),
    'default_festival_year' => env('BAZA_DEFAULT_FESTIVAL_YEAR'),
];
